{
  "name": "ai-search-planning-playground",
  "version": "1.0.0",
  "private": true,
  "description": "AI Search & Planning Playground (Python Backend + React JSX Frontend)",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "server": "py main.py 8000"
  }
}
